from decouple import config
USERNAME=config("USER")
BOTNAME=config("BOTNAME")
initial_statement=["working on it","hold on the result is almost ready","the tasks is almost completed","process is almost ready"]